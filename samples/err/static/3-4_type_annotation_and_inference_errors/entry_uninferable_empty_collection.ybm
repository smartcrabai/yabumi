# Verifies: SPEC §3.4 (scope of required type annotations) / D-TYPE-15 uninferable-type
# conditions (1. empty list/dict/set) / D-DIAG-02 E1003
# A bare empty-list-literal initialization `xs = []` written outside a context where the
# expected type is determined (D-TYPE-16's 4 contexts) is uninferable, producing E1003.

xs = []
print(xs.len())
