# Follows the schema in SAMPLES_PLAN.md section 1.3.
# Verifies SPEC 11.2 / STDLIB.md §9 time via a fixed-epoch_ms format/parse round-trip,
# and confirms only time.sleep's void type and time.now()'s int type (without verifying the value's non-determinism).

[[case]]
id = "run_ok"
entry = "entry_main.ybm"
cmd = "run"
args = []
stdin_file = ""
exit_code = 0
diagnostics = []
fmt_diff_expected = false
fmt_result_file = ""
stdout = { mode = "exact", value = "time sample done\n" }
stderr = { mode = "exact", value = "" }
doc_blocks = []
requires_env = []
notes = "Confirms a format/parse round-trip at a fixed epoch_ms, confirms only sleep's type, and confirms only time.now()'s int type (the value is not verified)."
