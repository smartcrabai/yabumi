# Follows the schema in SAMPLES_PLAN.md section 1.3.
# Verifies the success cases of SPEC 11.2 / STDLIB.md §12's math module. Only 1 case.

[[case]]
id = "run_ok"
entry = "entry_main.ybm"
cmd = "run"
args = []
stdin_file = ""
exit_code = 0
diagnostics = []
fmt_diff_expected = false
fmt_result_file = ""
stdout = { mode = "exact", value = "math ok\n" }
stderr = { mode = "exact", value = "" }
doc_blocks = []
requires_env = []
notes = "Asserts the success cases of checked_div/checked_mod/checked_add/checked_sub/checked_mul, asserts abs_int/abs_float/min_int/max_int/min_float/max_float/floor/ceil/round/sqrt/pow with fixed inputs, and asserts math.PI/math.E against a range of known approximate values."
