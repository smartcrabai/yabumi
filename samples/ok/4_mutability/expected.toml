# Follows the schema in SAMPLES_PLAN.md §1.3. ok/4_mutability: 1 case.

[[case]]
id = "mutability_basics"
entry = "entry_main.ybm"
cmd = "run"
args = []
stdin_file = ""
exit_code = 0
diagnostics = []
fmt_diff_expected = false
fmt_result_file = ""
stdout = { mode = "exact", value = "" }
stderr = { mode = "exact", value = "" }
doc_blocks = []
requires_env = []
notes = "Asserts reassigning var y (contrasted with immutable-by-default x), rewriting a field on a var struct, destructive push/pop calls on a var list, and subscript assignment xs[i] = v / m[k] = v on a var list/dict (D-COL-02)."
