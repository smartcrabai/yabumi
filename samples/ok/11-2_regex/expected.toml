# Follows the schema in SAMPLES_PLAN.md section 1.3.
# Verifies SPEC 11.2 / STDLIB.md §11 regex's is_match/find/find_all/replace/
# replace_all/captures with a fixed pattern and fixed input.

[[case]]
id = "run_ok"
entry = "entry_main.ybm"
cmd = "run"
args = []
stdin_file = ""
exit_code = 0
diagnostics = []
fmt_diff_expected = false
fmt_result_file = ""
stdout = { mode = "exact", value = "regex sample done\n" }
stderr = { mode = "exact", value = "" }
doc_blocks = []
requires_env = []
notes = "Verifies the Ok-side value of is_match/find/find_all/replace/replace_all/captures using [0-9]+/([a-z]+)([0-9]+) against \"ab12cd34\"."
