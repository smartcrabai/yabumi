[[case]]
id = "run_ok"
entry = "entry_main.ybm"
cmd = "run"
args = []
stdin_file = ""
exit_code = 0
diagnostics = []
fmt_diff_expected = false
fmt_result_file = ""
stdout = { mode = "exact", value = "" }
stderr = { mode = "exact", value = "" }
doc_blocks = []
requires_env = []
notes = "Asserts ? inside a Result-returning function, ? inside an Option-returning function, postfix ? on each pipe stage, and ? inside a lambda body (D-ERR-02, matched against the return type determined from the call context)."
