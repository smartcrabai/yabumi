# Follows the schema in SAMPLES_PLAN.md section 1.3.
# Verifies SPEC 14's memory model (value semantics, var self as the sole
# mutation-propagation path, closure value capture). Only 1 case.

[[case]]
id = "run_ok"
entry = "entry_main.ybm"
cmd = "run"
args = []
stdin_file = ""
exit_code = 0
diagnostics = []
fmt_diff_expected = false
fmt_result_file = ""
stdout = { mode = "exact", value = "memory model value semantics ok\n" }
stderr = { mode = "exact", value = "" }
doc_blocks = []
requires_env = []
notes = "Asserts non-propagation when passing struct/list as function arguments, that only a var self method call propagates, and that a lambda's var capture is a value copy."
