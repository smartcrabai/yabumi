# Follows the schema in SAMPLES_PLAN.md section 1.3.
# Verifies SPEC 11.2 / STDLIB.md §5 fs's read/write/append/list/exists/remove.
# The _out/ sandbox is cleaned up by the harness before and after the run (section 1.4).

[[case]]
id = "run_ok"
entry = "entry_main.ybm"
cmd = "run"
args = []
stdin_file = ""
exit_code = 0
diagnostics = []
fmt_diff_expected = false
fmt_result_file = ""
stdout = { mode = "exact", value = "fs sample done\n" }
stderr = { mode = "exact", value = "" }
doc_blocks = []
requires_env = []
notes = "Verifies a sequential flow of write/read/append/read/exists/list/remove/exists against _out/greeting.txt."
